import { SignupForm } from "@/components";

function Page() {
    return (
        <div className="min-h-screen flex items-center justify-center p-4">
            <SignupForm />
        </div>
    );
}

export default Page;
