import { LoginForm } from "@/components";

function Page() {
    return (
        <div className="min-h-screen flex items-center justify-center p-4">
            <LoginForm />
        </div>
    );
}

export default Page;
