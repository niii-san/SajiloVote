import {
    asyncHandler,
    ErrorResponse,
    SuccessResponse,
} from "../../utils/index.js";
import { Request, Response } from "express";

export const signup = asyncHandler(async (req: Request, res: Response) => { });
