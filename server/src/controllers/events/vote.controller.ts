import { Request, Response } from "express";
import {
    asyncHandler,
    ErrorResponse,
    SuccessResponse,
} from "../../utils/index.js";
import {
    Event,
    VoteCandidate,
    VoteRecord,
} from "../../models/index.js";


export const voteForCandidate = asyncHandler(async(req:Request,res:Response)=>{







})

export const voteForOption = asyncHandler(async(req:Request,res:Response)=>{





})
